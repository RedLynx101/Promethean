# Phase 3 — Sample Outputs

Exported artifacts and representative outputs generated by Promethean during evaluation. Keep this folder small — the authoritative JSON lives in [../traces/](../traces/).

Suggested contents:

- `tc-01_final_graph.png` — exported graph view of the deployed CRM workflow
- `tc-01_governance_config.json` — final governance config (thresholds, alert rules)
- `tc-02_final_graph.png`
- `tc-02_governance_config.json`
- One or two representative `execution` records from a post-deploy run (if the system has executed any deployed workflows)

This folder maps to the course-recommended `outputs/` directory.
